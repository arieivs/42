#include "conversion.hpp"

int	check_type(std::string arg) {
	/*if (arg.length() == 1 && ((arg[0] >= 9 && arg[0] <= 13) ||
		(arg[0] >= 32 && arg[0] <= 47) || (arg[0] >= 58 && arg[0] <= 126)))*/
	std::size_t	i = 0;

	if
	while (i != std::string::npos) {
		i++;
	}
}
